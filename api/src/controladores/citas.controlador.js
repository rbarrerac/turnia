/**
 * Módulo: Controlador de citas — disponibilidad, reservas, historial, agenda y estados
 * Proyecto: Turnia
 * Autor: Ronald
 * Fecha de creación: 15/09/2026
 */

export async function obtenerDisponibilidad(peticion, respuesta) {
  respuesta.json({ exito: true, datos: [], mensaje: 'Pendiente de implementar' });
}

export async function crear(peticion, respuesta) {
  respuesta.json({ exito: true, datos: null, mensaje: 'Pendiente de implementar' });
}

export async function listarMias(peticion, respuesta) {
  respuesta.json({ exito: true, datos: [], mensaje: 'Pendiente de implementar' });
}

export async function cancelar(peticion, respuesta) {
  respuesta.json({ exito: true, datos: null, mensaje: 'Pendiente de implementar' });
}

export async function obtenerAgenda(peticion, respuesta) {
  respuesta.json({ exito: true, datos: [], mensaje: 'Pendiente de implementar' });
}

export async function cambiarEstado(peticion, respuesta) {
  respuesta.json({ exito: true, datos: null, mensaje: 'Pendiente de implementar' });
}
